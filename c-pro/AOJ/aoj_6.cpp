#include <bits/stdc++.h>
using namespace std;

struct Point {
    double x, y;
};

void solve(int n, Point a, Point b) {
    if (n == 0) return;
    Point s, t, u;
    return ;
}

int main() {
    int n; cin >> n;
    vector<int> N(n);
    for (int i = 0; i < n; ++i) cin >> N.at(i);
    int q; cin >> q;
    vector<int> Q(q);
    for (int i = 0; i < q; ++i) cin >> Q.at(i);


    return 0;
}