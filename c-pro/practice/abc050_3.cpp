#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ll N; cin >> N;
    vector<ll> abs(N);

    for (int i = 0; i < N; ++i) {
        cin >> abs.at(i);
    }

    
       
    return 0;
}