#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int N; cin >> N;
    vector<string> S(N);
    for (int i = 0; i < N; i++) {
        string s; cin >> s;
        S[i] = s;
    }
    
    return 0;
}