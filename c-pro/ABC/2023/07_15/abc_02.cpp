#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, M; cin >> N >> M;
    vector<pair<int, vector<int>>> products(N);
    for (auto &&[price, feature]: products) {

    }
    
    return 0;
}