#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define MAX 1000000000

int main() {
    int N; cin >> N;
    vector<pair<ll, ll> > Data(N);
    for (int i = 0; i < N; i++) {
        cin >> Data[i].first >> Data[i].second;
    }

    return 0;
}